export const colors = {
    white: '#fff',
    black: '#000',
} as const;

export const theme = {
    colors,
    header: {
        padding: '6em',
        height: '11.12em',
    },
} as const;

export default theme;